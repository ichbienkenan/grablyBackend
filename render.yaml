services:
  - type: web
    name: grably
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip
      pip3 install yt-dlp
      npm install
    startCommand: node server.js
